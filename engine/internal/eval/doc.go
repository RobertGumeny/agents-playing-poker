// Package eval loads completed session artifacts and derives the offline eval.json
// summary used by experiment comparison and reporting.
//
// The stable eval.json schema lives in docs/session-artifacts.md.
package eval
