{
  "name": "@agent-poker/pi-agents-workspace",
  "private": true,
  "workspaces": [
    "shared",
    "llm-stateless"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspace @agent-poker/pi-agent-shared"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
